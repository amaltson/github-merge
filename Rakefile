# -*- ruby -*-

require "rubygems"
require "hoe"

# Hoe.plugin :compiler
# Hoe.plugin :gem_prelude_sucks
# Hoe.plugin :inline
# Hoe.plugin :racc
# Hoe.plugin :rcov
# Hoe.plugin :rubyforge
Hoe.plugin :bundler

Hoe.spec "github-repo-merge" do
  # HEY! If you fill these out in ~/.hoe_template/Rakefile.erb then
  # you'll never have to touch them again!
  # (delete this comment too, of course)

  # developer("FIX", "FIX@example.com")

  # self.rubyforge_name = "github-repo-mergex" # if different than "github-repo-merge"

  self.author = "Arthur Maltson"
  self.email = "arthur.kalm@gmail.com"

  self.dependency "github_api", "~> 0.9.4"
  self.dependency "thor", "~> 0.18.1"
  # license "MIT" # this should match the license in the README
end

# vim: syntax=ruby
